#include<stdlib.h>
#include<stdio.h>
/**
 * main - prints name of program
 * @argc: number of parametsr
 * @argv: parameters being held
 * Return:Always 0 success
 */
int main(int __attribute((unused)) argc, char *argv[])
{
	int i = 0;

	printf("%s\n", argv[i]);

	return (0);
}
